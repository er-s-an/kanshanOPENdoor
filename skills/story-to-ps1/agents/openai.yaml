interface:
  display_name: "Story to PS1"
  short_description: "Build and validate authorized story-to-PS1 packages"
  default_prompt: "Use $story-to-ps1 to turn this authorized story excerpt into a validated PS1 playable package."
policy:
  allow_implicit_invocation: true
