# 复制为 .env 并按需填写（.env 已被 gitignore，绝不入库）。
# Secret 只从本地环境 / 文件读取，绝不进代码或日志。

# 知乎直答 Access Secret。留空且无本地开发文件时，管线自动以 mock 模式跑通。
ZHIHU_ACCESS_SECRET=

# 本地开发 secret 文件路径（默认读取仓库根目录 .access_secret）。
# 显式设成空（ZHIHU_ACCESS_SECRET_FILE=）可强制“无 key”，等价离线 mock。
# ZHIHU_ACCESS_SECRET_FILE=

# 知乎直答 OpenAI 兼容端点（一般不用改）
# ZHIHU_BASE_URL=https://developer.zhihu.com/v1

# 模型：分析/生成用 thinking；简单步骤（刘看山引导文案）用 fast
# ZHIHU_MODEL_THINKING=zhida-thinking-1p5
# ZHIHU_MODEL_FAST=zhida-fast-1p5

# 强制 mock（即使有 key），用于演示与测试
# ZHIHU_MOCK=1
