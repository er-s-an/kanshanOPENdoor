# 看山任意门 · 游戏体验重构施工计划（2026-09-14）

## 目标与状态口径

把已入库素材从背景装饰变成玩家可感知、可操作、可推理的游戏对象，优先完成《蓝血》从刘看山接站到人格卡回流的完整竖切，同时保持另外两个故事可进入、可游玩。

玩家旅程以 [docs/PLAYER-JOURNEY.md](docs/PLAYER-JOURNEY.md) 为基线；调查人格以 [docs/INVESTIGATOR-PERSONAS.md](docs/INVESTIGATOR-PERSONAS.md) 为基线；最终以 [docs/PLAYER-QA-20260914.md](docs/PLAYER-QA-20260914.md) 做新玩家验收。主流程按状态推进，不规定目标游玩时长。

本文件的“已实现”指当前 `main` 已有代码、内容或素材，并有自动化或浏览器验证支持；公网关键路径与本地桌面整链已通过，但不能替代 375px 整章与目标玩家盲测。

本轮不改 `pipeline/stories/api/**`，不替原作开放片段补写幕后真相，也不把模型自由文本作为线索授予、人格判型或胜负裁决依据。

## 2026-09-14 本地集成实证

### 已实现

- **接站、选门、开门**：根路径先显示刘看山接站页，玩家确认后才看故事库；普通 `?story=` 只突出对应故事，只有 `?scene=` 评审暗门可直达。玩家点击真实故事按钮后才播放开门转场。
- **故事简报**：新存档在第一幕前显示刘看山三句简报，分别说明身份、异常和第一步；可跳过、可换门，续玩不会重复阻塞。
- **社区场景**：《蓝血》已有可玩的 `post` 类型、1 个 14 条回应的帖子流、4 个可追查账号及各自的评论/私信详情。错误账号不会授证或造成死路。
- **场景与搜证**：阅读、聊天、调查、行动、公开论证和结局均可消费场景图；未发现物件不预列，确定性搜索、语义热点、证据纸、查证与错误反馈已经接通。
- **公开论证**：原 Boss 已改成“选主张 → 逐项回应质疑 → 查看证据边界 → 阶段收束”；无关或越界证据不消耗，进度可恢复。
- **八型调查人格**：《蓝血》三个自然必经二选一分别写入证物/人证、追击/克制、公开/隐蔽三轴。稳定 `choiceId` 支持回放，穷举测试证明八种组合全部可达，判型依据可回指本局真实选择。
- **人格素材与分享**：8 张专属刘看山透明人格图已接入。结局可导出 Canvas `1080×1440` PNG，调用 Web Share；不支持系统分享时复制真实入口。链接只携带故事、来源和人格代码，不携带存档或用户资料。
- **外部能力降级**：没有 LLM、OAuth 或动态热榜时，固定对白、人格文案、保存图片和主流程仍可工作。
- **网关额度保护**：请求指纹磁盘缓存、同键并发合并、原子缓存写入、客户端窗口限流与单实例每日上游预算已经落地。缓存命中与同键跟随请求不消耗上游名额；失败与不完整响应不写长期缓存。

### 当前验证

- `npm test`：78/78 通过，包含社区场景、三轴八型穷举、匿名分享链接、拒答兜底、网关缓存/并发/限流/每日配额、搜证与公开论证规则。
- `npm run typecheck`：通过。
- `npm run build`：通过。
- Ego 已从本地干净根路径完整走到人格卡，并在 canonical 公网实点接站、选门、简报、NPC 对话及 375px 社区/人格卡；两个公网域名与本地生产构建资源指纹一致。

## 外部接口与缓存决策

不建设 Redis、数据库或通用缓存平台。当前是单机网关加隧道的比赛版本，文件缓存和进程内保护足够；目标是减少重复上游调用、守住当日额度并保证失败时剧情仍可继续。

- **额度按保守口径设计**：仓库内开放平台文档写的是全网搜索 5,000 次/日、知乎搜索 5,000 次/日、知乎热榜 100 次/日、知乎直答 100 次/日。正式接入前只以个人中心「用量统计」为准；未确认时按直答 100 次/日处理。
- **故事正文**：20 篇开放故事已经落在 `pipeline/stories/api/`，运行时只读本地编译结果，不再请求故事接口。
- **直答与兼容模型缓存**：`game/server/.cache/` 按故事、场景、上游地址、模型、系统提示词和完整消息生成请求指纹；只缓存成功且完整的回复。故事、提示词、模型或上游改变时自然隔离。
- **同键并发合并**：多个标签页或重复点击同时产生相同 cache miss 时，只发起一次上游调用，其余请求等待同一结果；显式 reroll 仍可要求新回复。
- **原子写入**：先写临时文件再重命名，避免并发或进程中断留下半截 JSON；不完整流、上游 429 和错误结果不缓存。
- **限流与每日预算**：默认每个来源地址 10 分钟最多触发 60 次真实上游调用；知乎直答上游默认每天最多 80 次，预留 20 次余量；兼容模型端点默认每天最多 4,500 次。所有值可通过服务端环境变量收紧，且只统计实际上游调用。
- **来源地址边界**：只有本机回环连接才信任 Cloudflare 的规范来源头；直接访问不能通过伪造 `X-Forwarded-For` 绕过限流。
- **搜索**：游戏内证据搜索使用编译期、经审核的来源快照，不让玩家查询直接消耗开放平台搜索额度。
- **热榜**：真实热榜尚未接入。接入时采用本地 JSON 缓存与 6 小时 TTL；刷新失败继续使用最后一次成功结果，并展示数据日期。
- **OAuth**：尚未接入。未来 token 只留在服务端会话，不写 localStorage、不写日志、不进入共享磁盘缓存；登录不成为首局门槛。
- **人格卡文案**：当前为确定性固定文案。以后如接 LLM，只允许改写语气，失败立即回退固定文案，不得影响判型和分享。

缓存验收不是“目录里有文件”，而是：相同请求不重复触发上游、并发相同请求只产生一次上游调用、配额或网络错误不会卡住剧情、失败响应不被长期缓存、任何缓存和日志中都没有 Secret 或 OAuth token。单实例内的保护已经有自动化覆盖；多实例共享配额不在本轮架构范围内。

## 玩家体验施工状态

### 1. 门厅与入戏：已实现，待最终全流程验收

- 公网产品路径设计为“刘看山接站 → 玩家选门 → 刘看山开门 → 故事简报 → 第一幕”。
- 普通 `?story=` 只预选并突出故事，不自动替玩家进入；`?scene=` 只用于内部评审。
- 开门素材只用于门厅进入剧本；新局故事简报使用刘看山现有授权素材，并支持减少动态。

### 2. 社区信息与私信：旗舰故事已实现

- 《蓝血》已接 `post` 场景，虚构账号和氛围楼层分开，并清楚标注知情边界。
- 玩家可以先查一位账号，再回到帖子查其他人；错误路径有回应且不扣资源。
- OAuth 不是入场门槛；真实关注答主不被写成虚构评论角色。

### 3. 场景舞台、搜证与证据纸：已实现基础闭环

- 场景图已覆盖主要叙事类型，图片失败不阻塞流程，键盘与文字路径保留。
- 搜证只匹配当前幕已满足前置条件的公开别名；唯一命中才授证，多命中消歧，无关查询不改变状态。
- 证据纸使用可复制、可缩放、可读屏的 DOM 文字，图片仅作底板。

### 4. 关系板：未完成

- 仍需把事实、证言、工作假设统一到一张可视化关系板。
- 关系板必须让玩家明确“材料支持什么、还缺什么”，不能恢复成百分比收集清单。

### 5. 公开论证：已实现规则与页面，待整链体验收口

- 玩家选择有边界的主张，再用证据回应质疑；反馈明确说明支持范围。
- `credibility` 表示论证质量，`exposure` 表示公开风险；赞同数只作演出。
- 无效或越界证据不消耗，玩家可收窄主张后继续。
- 越界主张即使答完三轮质疑也必须回草稿收窄，不能用分数覆盖主张本身的证据边界。

### 6. 结局、调查人格与分享：已实现基础闭环

- 结局先展示阶段结果和本局选择，再揭晓调查人格。
- 八型只由必经三轴选择决定；8 张专属透明人格图均已接入。
- 分享卡可保存 `1080×1440` PNG、调用 Web Share 或复制匿名回流链接。
- 当前回流页由刘看山接站并突出对应故事；二维码、独立人格结果落地页和 LLM 语气改写不是主路径依赖。

### 7. 复盘、登录与动态数据：未完成

- 反事实复盘尚未实现，结局还不能指出“另一条路线差在哪一步”。
- OAuth 与真实热榜尚未接入；必须继续作为可选增强。

## 施工波次

1. **Wave A，已完成**：冻结 `types / rules / engine / boss-rules` 共享契约。
2. **Wave B，已完成并集成到本地 main**：门厅接站、开门、故事简报、社区场景、场景舞台、搜证、证据纸、公开论证、八型规则、8 张人格图与分享卡。
3. **Wave C，部分完成**：自动化 78/78、typecheck、build 与桌面 Ego 新存档整链已通过；仍需关系板、反事实复盘、375px 整章、768px、全键盘和输入法复验。
4. **Wave D，已完成关键验收**：当前生产构建已在 8791 网关与 canonical/备用域名上线；资源指纹、故事数据、代表性素材和公网关键路径一致。
5. **玩家验证，未完成**：邀请 5–8 名未看文档的目标玩家盲测，记录卡住位置、错误理解和是否愿意保存/分享。

## Go / No-Go

当前结论：**可玩竖切与公网演示为 Go；对“完整体验已完成玩家验证”仍是 No-Go。**

发布口径需要同时满足：

- 从公网根路径用新存档走完“接站 → 选门 → 开门 → 简报 → 社区/私信 → 搜证/查证 → 关系板 → 公开论证 → 结局 → 人格卡 → 保存/分享”，不使用 `?scene=`。
- 事实、证言、工作假设在统一关系板中可理解，反事实复盘能回指真实选择。
- 错误搜索不授证、错误呈证不软锁、刷新可恢复进度；LLM、OAuth 与热榜关闭时仍可完成。
- 375px、768px、1440px 无主流程溢出；触控目标至少 44px；键盘可完成关键操作；减少动态下无不可跳过等待。
- 5–8 名目标玩家盲测完成，并据真实卡点收口。
- canonical 公网 URL 的首页、故事数据、代表性素材、bundle、人格保存与匿名回流均验证为当前集成版本。

在这些条件全部满足前，可以说“本地核心流程已实现并进入验收”，不能说“全链路已经发布”或“玩法有趣已经被证明”。
