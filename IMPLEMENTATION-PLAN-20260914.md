# 看山任意门 · 游戏体验重构施工计划（2026-09-14）

## 目标

把已入库素材从“背景装饰”变成玩家可感知、可操作、可推理的游戏对象，优先完成《蓝血》从门厅进入到阶段结局的完整竖切，同时保持另外两个故事可进入、可游玩。

玩家旅程以 [docs/PLAYER-JOURNEY.md](docs/PLAYER-JOURNEY.md) 为基线；调查人格与分享卡以 [docs/INVESTIGATOR-PERSONAS.md](docs/INVESTIGATOR-PERSONAS.md) 为基线。主流程按状态推进，不规定目标游玩时长。

本轮不改 `pipeline/stories/api/**`，不替原作开放片段补写幕后真相，也不把模型自由文本作为线索授予或胜负裁决依据。

## 当前实证

- 31 幕《蓝血》配置了 16 个唯一场景图片路径，但 `Art` 只在 `ChatView` 中渲染；阅读、调查、Boss、结局的大多数图片对玩家不可见。
- 调查页预先列出全部可看物件；搜索只是过滤这张已公开清单，不能算“搜索发现证据”。
- 4 张透明纸张目前只在 Boss 中作为空白缩略图，文字在纸外；线索簿与首次发现没有使用。
- 开门转场在普通换场时触发，真正进入剧本时反而没有正确绑定。
- 当前 Boss 是“猜唯一对象 + 每轮猜一张白名单卡 + 涨赞”；错误线索会跨轮消耗并造成软锁，状态刷新丢失。
- 《蓝血》终幕复盘只支持“值得继续追查”的工作假说；现有 Boss 却越级定案为组织甄别，与故事事实边界冲突。

## 外部接口与缓存决策

不建设 Redis、数据库或一套通用缓存框架。当前是单机网关加隧道的比赛版本，文件缓存已经够用；缓存的目的首先是保证演示稳定和内容可复核，其次才是节省额度。

- **额度按保守口径设计**：仓库内开放平台文档写的是全网搜索 5,000 次/日、知乎搜索 5,000 次/日、知乎热榜 100 次/日、知乎直答 100 次/日；`STATUS.md` 里“直答 5,000”只是一次历史快照。正式接入前只以个人中心「用量统计」为准，在未确认前按 100 次/日保护直答。
- **故事正文**：20 篇开放故事已经落在 `pipeline/stories/api/`，运行时只读本地编译结果，不再请求故事接口，也不另做浏览器缓存。
- **直答与其它兼容模型**：保留 `game/server/.cache/` 的请求指纹磁盘缓存。缓存键继续包含故事、场景、模型、系统提示词和完整消息；只缓存成功响应，故事/提示词/模型改变时自然失效。接回知乎直答前再补“同键并发合并”和服务端冷却/限流，避免同一时刻的重复 miss 与公开链接被刷；客户端“换一种说法”不能无限绕过缓存。
- **搜索**：游戏中的证据搜索使用编译期、经审核的来源快照，不让每个玩家在运行时任意消耗搜索额度。查询或来源版本改变时由内容管线手动刷新。
- **热榜**：真正接入时才增加一个本地 JSON 缓存，TTL 为 6 小时；过期后最多刷新一次，刷新失败继续使用最后一次成功结果并明确显示数据日期。这样单实例理论上每天最多 4 次刷新。
- **OAuth 用户资料与关注列表**：token 只留在服务端会话，不写 localStorage、不写日志、不进入共享磁盘缓存。若实际接入后发现重复拉取，再按用户会话做 15 分钟内存缓存，登出、授权失效或 token 轮换时立即清除；登录与首局主流程不依赖缓存命中。
- **人格卡文案**：结局确定后每个存档只生成一次并随存档复用；接口失败直接使用八型固定文案，不重试到阻塞分享。

缓存验收不是“目录里有文件”，而是：相同请求不重复触发上游、并发相同请求只产生一次上游调用、配额/网络错误不会卡住剧情、失败响应不被长期缓存、任何缓存和日志中都没有 Secret 或 OAuth token。

## 体验协议

### 1. 门厅与入戏

- 公网根路径先显示刘看山接站页，再由玩家亲手选择故事；《蓝血》可以置顶突出，但不能默认进入。普通 `?story=` 只预选对应门，只有评审 `?scene=` 暗门直达。
- 刘看山是会出现、会说话、会开门的引路人，不是第二张门厅背景或短暂加载图。
- 推门后用三句话以内说明身份、异常与第一步，并让玩家在真实场景完成第一个动作。
- 开门素材只用于“门厅 → 剧本”；故事内部使用短淡化，不重复开门。
- 入场转场图片预加载；减少动态时跳过非必要动画。

### 2. 社区信息与私信

- 新增 `post` 场景，把剧情时序内的帖子、评论楼、关键词记录、人物回复和私信入口组织在同一信息层。
- 首个旗舰场景使用 18–30 条可读评论、3–5 个关键虚构账号；玩家不需要翻完全部内容。
- 回错人不扣资源：对方按知情范围回应，刘看山帮助缩小范围。
- OAuth 不是入场门槛；真实关注答主不被写成剧情评论者。

### 3. 场景舞台

- 阅读、聊天、调查、行动、公开论证、结局都消费 `scene.image`。
- 阅读/结局使用固定比例的叙事主图；调查使用 `object-fit: contain` 的可操作画面，避免热点因裁切漂移。
- 图片失败时不阻塞流程；所有交互保留键盘与文本替代路径。

### 4. 搜证与证据

- 未发现物件不预先显示名称、正文、线索描述或数量线索。
- 玩家可点场景热点，或输入关键词执行确定性搜索；唯一命中才发现，多命中进入消歧，空/无关查询不改变状态。
- 搜索只匹配本幕已满足前置条件的公开别名，不检索隐藏观察正文或验证答案。
- 线索簿只搜索已发现记录，不再泄露未发现元数据。
- 透明纸张是 UI 底板；名称、摘要、来源均为可复制、可缩放、可读屏的 DOM 文本。
- 证据板区分事实、证言和工作假设；不以“100% 修复度”催玩家清空所有可选内容。

### 5. 公开论证（原 Boss）

- 玩家先选择本次愿意负责的主张，再组合证据回应逐轮质疑。
- 每次反馈明确写出“这能证明什么 / 仍不能证明什么”；强证据与弱证据结果不同。
- 只保留 `credibility`（论证可信度）与 `exposure`（公开风险）两个有玩法意义的指标；赞同数只作演出。
- 无效证据不永久消耗；越界指控允许收窄重写；主动撤回不能被写成平台折叠。
- Boss 进度进入存档，刷新后可以继续。
- 结局保留“公开留档 / 保护性追查 / 暂停 / 更大的问题”等阶段选择，不宣告幕后真相。

### 6. 结局、调查人格与分享回流

- 先展示阶段结果、关键因果与仍未知的问题，再揭晓八型调查人格。
- 主人格只由必经选择的三轴决定；可选探索只产生附加印章；LLM 只填写卡片留白文案。
- 八张刘看山透明角色图不包含文字；前端排版并导出 `1080×1440` PNG。
- 图片保留案件二维码/短链接；分享链接落地先展示朋友的人格卡，再一步进入对应故事。
- 没有 LLM、OAuth 或动态热榜时，固定文案、保存图片和主流程仍可工作。

## 施工波次与文件所有权

1. Wave A：单负责人冻结 `types / rules / engine / boss-rules` 共享契约。
2. Wave B：按不重叠文件并行完成场景舞台、调查搜索、证据纸、Boss、门厅、故事数据与自动测试。
3. Wave C：集成人统一 typecheck、测试、构建、浏览器全流程、响应式与无障碍检查。
4. Wave D：同步到运行副本，精确重启 8791 网关，验证 canonical 公网 URL；不并行运行 `start-demo.sh`。

## Go / No-Go

必须满足以下条件才可称为实现完成：

- typecheck 通过，game/root 测试全绿；新增搜索、Boss 规则、素材契约测试通过。
- 《蓝血》可从新存档走完“刘看山接站 → 玩家选门 → 开门 → 故事内引导 → 处境 → 社区/私信 → 搜证/查证 → 证据板 → 公开论证 → 结局 → 调查人格 → 保存卡片”。
- 错误搜索不授证、错误呈证不软锁、刷新可恢复 Boss。
- 375px、768px、1440px 下无主流程溢出；触控目标至少 44px；键盘可完成搜证与对质。
- reduced-motion 下无视差、数字滚动或不可跳过等待。
- 线上首页、故事 JSON、代表性素材、bundle 与关键流程均验证成功。

“玩法有趣”仍需 5–8 名目标玩家盲测后才能下结论；本轮可以证明实现与体验意图一致，不能用开发者自测替代真实玩家证据。
